void main(){
	println("Enter a temperature in Celsius: ");
	double tempC = toDouble(input());
	double tempF = celsiusToF(tempC);
	println(toStr(tempC) + " Celsius is " + toStr(tempF) + " in Fahrenheit.");
}

double celsiusToF(double tempC)
{
	return tempC * 1.8 + 32.0;
}